Describe the problem and the resulting behavior.

Validation:

- [ ] `npm ci`, `npm run typecheck` and `npm run build` pass.
- [ ] UI changes checked in fixture states at 240 and 440 px, including keyboard focus.
- [ ] Host claims identify SDK declarations, live evidence or an unverified assumption.

Include a screenshot for UI changes and list any remaining live Forma checks.
